import { RepchangeDirective } from './repchange.directive';

describe('RepchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new RepchangeDirective();
    expect(directive).toBeTruthy();
  });
});
